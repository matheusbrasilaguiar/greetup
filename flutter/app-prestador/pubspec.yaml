name: app_prestador
description: GreetUp — App Prestador (Cozinha, Garçom, Display)
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  go_router: ^14.0.0
  provider: ^6.1.2
  socket_io_client: ^2.0.3+1
  flutter_svg: ^2.0.10+1
  wakelock_plus: ^1.2.10
  http: ^1.2.0
  google_fonts: ^6.2.1

dependency_overrides:
  package_info_plus: ^8.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
